# aria2 1.37.0 — corresponding source

- **Upstream tag:** `release-1.37.0` — <https://github.com/aria2/aria2/releases/tag/release-1.37.0>
- **License:** GPLv2 (see [`../GPLv2.txt`](../GPLv2.txt))
- **Shipped in:** Fetchii (bundled `aria2c`), macOS universal2 (arm64 + x86_64), static
- **TLS:** Apple Secure Transport (`--with-appletls`) — no OpenSSL/GnuTLS linked

## Get the source

```sh
curl -L -o aria2-1.37.0.tar.xz \
  https://github.com/aria2/aria2/releases/download/release-1.37.0/aria2-1.37.0.tar.xz
# sha256 recorded at build time:
# <SHA256 auto-filled by build-aria2.yml>
```

## Build (per arch, then lipo into universal2)

```sh
./configure \
  --host=arm64-apple-darwin \
  --without-libxml2 --without-libexpat --without-sqlite3 \
  --without-libssh2 --without-libcares --without-libnettle \
  --without-libgmp --without-libgcrypt \
  --with-appletls \
  CFLAGS="-arch arm64 -O2" CXXFLAGS="-arch arm64 -O2" LDFLAGS="-arch arm64"
make -j$(sysctl -n hw.ncpu)
# repeat for x86_64, then:
# lipo -create arm64/aria2c x86_64/aria2c -output aria2c
```

> This record is generated by the `build-aria2.yml` workflow; the `<SHA256 …>`
> placeholder is filled in automatically from the downloaded source tarball.
